import "package:flutter/material.dart";

class CommunityS extends StatelessWidget {
  const CommunityS({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      backgroundColor: Colors.green,
    );
  }
}